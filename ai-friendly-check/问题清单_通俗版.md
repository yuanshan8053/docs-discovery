# KOMOJU 文档问题清单(通俗版)

> 对 KOMOJU 全套 164 篇英文文档的 AI 友好度质检,共发现 **31 条**问题(高 22 · 中高 7 · 中 2)。
> 下表按严重度排序,每条都配了「大白话说明」和原文跳转链接,点链接即可到出问题的那篇文档。

| 严重度 | 类别 | 问题一句话 | 通俗说明 | 原文跳转 |
|---|---|---|---|---|
| 高 | API字段/通用写作 | **字段名拼错了** | 响应里表示「条形码」的字段名写成了 `bardcode`,正确拼法是 `barcode`(少打了一处),而且旁边的说明写的就是「Barcode number」。程序按正确拼法去读会读不到值。这个错拼在 14 篇接口文档里都出现了。 | [capturepayment](https://doc.komoju.com/reference/capturepayment.md) |
| 高 | API字段/通用写作 | **字段名拼错了** | iDEAL 支付明细里表示「交易码」的字段名写成 `transacion_key`,正确应是 `transaction_key`(漏了个 t);同一篇里其他支付方式却是对的。这个错拼在 14 篇接口文档里都出现了。 | [capturepayment](https://doc.komoju.com/reference/capturepayment.md) |
| 高 | API字段/通用写作 | **说明和实际对不上** | 韩国卡有个「银行卡密码前几位」的字段,字段名和格式限制都写明是「前 2 位」,但文字说明却写成「前 4 位」,自相矛盾,填几位说不清。 | [updatepayment](https://doc.komoju.com/reference/updatepayment.md) |
| 高 | 通用写作 | **接口说明抄错了** | 「支付会话」这个接口,成功返回的说明却写成「Cancels the session.(取消会话)」。程序会误以为「成功=取消」,完全判反。 | [paysession](https://doc.komoju.com/reference/paysession.md) |
| 高 | 跨文档一致性/通用写作 | **接口说明抄错了** | 「查看文件」和「查看申请状态」两个查询接口,成功返回的说明都被错抄成「Creates a new file(新建文件)」,和接口实际功能完全不符。 | [showfile](https://doc.komoju.com/reference/showfile.md)<br>[showliveapplication](https://doc.komoju.com/reference/showliveapplication.md) |
| 高 | API字段/通用写作 | **可选值列漏了一个** | 查询支付列表时,能按状态筛选,但可选状态只列了 6 个,漏掉了 `failed`(失败);而同一篇的数据定义里其实有这第 7 个状态。程序不知道能不能按「失败」筛。 | [listpayments](https://doc.komoju.com/reference/listpayments.md) |
| 高 | API字段 | **示例值不符合自己的规定** | 商户 ID 规定必须是 25 位,但文档给的示例值只有 22 位,违反了自己写的规则。 | [listpayments](https://doc.komoju.com/reference/listpayments.md) |
| 高 | 跨文档一致性/通用写作 | **同一个词前后不一致** | 同一篇文档里,状态列表写成 `complete`,但代码和正文却写 `completed`(多了 d)。程序拿去比对字符串会对不上。 | [hosted-page-integration-guide](https://doc.komoju.com/docs/hosted-page-integration-guide.md) |
| 高 | 通用写作 | **同一字段两种类型** | 同一个 `capture` 字段,一处说它取值是「auto/manual」两个词,另一处又说它是 true/false 布尔值。两种定义互不兼容,不知道该传什么。 | [creating-payments-directly](https://doc.komoju.com/docs/creating-payments-directly.md) |
| 高 | API字段/通用写作 | **三个字段说明一模一样** | `bank_id`、`customer_id`、`confirmation_id` 三个不同字段,说明文字完全相同(都写成同一句),读者根本分不清各自是干嘛的。 | [payment-parameter-description](https://doc.komoju.com/reference/payment-parameter-description.md) |
| 高 | 代码示例 | **示例代码是坏的,跑不了** | 有 4 段示例请求代码里,金额键写成了 `"amount'`(左边双引号、右边单引号,引号配错),还有多余的逗号。任何程序去解析都会直接报错。 | [platform-processing-model](https://doc.komoju.com/docs/platform-processing-model.md) |
| 高 | 代码示例 | **示例代码是坏的,跑不了** | 同上,有 2 段示例请求代码里金额键 `"amount'` 引号配错并带多余逗号,复制过去无法运行。 | [seller-processing-model](https://doc.komoju.com/docs/seller-processing-model.md) |
| 高 | 代码示例 | **示例里请求和返回对不上** | 示例请求填的名字是「Taro」,同一个示例的返回却显示「Tar」(少了个 o),对照会让人以为名字被截断了。 | [payment-details-example-bank-transfer](https://doc.komoju.com/recipes/payment-details-example-bank-transfer.md) |
| 高 | 代码示例 | **示例里请求和返回对不上** | 示例请求填的邮箱是 customer@example.com,同一示例返回却是 test@example.com,前后对不上。 | [payment-details-example-konbini](https://doc.komoju.com/recipes/payment-details-example-konbini.md) |
| 高 | 代码示例 | **示例里请求和返回对不上** | 示例请求填的卡有效期是 04/28,返回却显示成 1 月 / 2025 年,前后完全对不上,无法照着核对。 | [tokenize-credit-card-details](https://doc.komoju.com/recipes/tokenize-credit-card-details.md) |
| 高 | 代码示例 | **示例 JSON 是坏的,跑不了** | 一个示例代码块里塞了两段独立的 JSON 数据,中间没有逗号也没用数组括起来,当成一个 JSON 解析必然报错。 | [tokenize-credit-card-details](https://doc.komoju.com/recipes/tokenize-credit-card-details.md) |
| 高 | 跨文档一致性 | **不同页面说法打架** | 「不填 tax 会怎样」在三处说法不一:两篇旧文档说「不填=默认按 10% 自动算税」,而更新日志(新版本)说「不填=税为 0」。看不同页会得出相反结论。 | [creating-payments-directly](https://doc.komoju.com/docs/creating-payments-directly.md)<br>[v2026-01-20](https://doc.komoju.com/changelog/v2026-01-20.md) |
| 高 | 跨文档一致性 | **状态清单缺一个** | 支付状态,权威的数据定义有 7 个(含 `failed` 失败),但给人看的状态说明表只列了 6 个,漏了「失败」。判断支付状态时会漏掉这一种。 | [payments](https://doc.komoju.com/reference/payments.md)<br>[listpayments](https://doc.komoju.com/reference/listpayments.md) |
| 高 | 跨文档一致性 | **两篇不同文档标题撞车** | 两篇主题不同的集成指南(一篇讲托管页、一篇讲前端字段渲染)用了完全一样的大标题「Integration Guide: Customer」,检索时分不清是哪一篇、容易取错。 | [integration-guide-customer](https://doc.komoju.com/docs/integration-guide-customer.md)<br>[integration-guide-customer-1](https://doc.komoju.com/docs/integration-guide-customer-1.md) |
| 中高 | 通用写作 | **链接指错了地方** | 文档里 3 处跳转链接的地址拼错或指向了错误的接口页(比如把「创建会话」拼成了 creatession),点进去到不了正确页面。 | [creating-payments-directly](https://doc.komoju.com/docs/creating-payments-directly.md) |
| 中高 | 通用写作 | **错误码表前后不一致** | 同一篇里有两张状态码表,401 的叫法不一样(一处 Not Authorized、一处 unauthorized);而且 HTTP 表还漏掉了 400/402/504 三个错误码。 | [errors](https://doc.komoju.com/docs/errors.md) |
| 中高 | 通用写作 | **同一样东西两个叫法** | 同一个凭证,第一步叫「Merchant ID」,后面又叫「Merchant UUID」,读者不确定是不是同一个东西。 | [getting-started-with-ec-cube](https://doc.komoju.com/docs/getting-started-with-ec-cube.md) |
| 中高 | 通用写作 | **参数名前后不一致** | 同一个参数,说明表里写驼峰式 `paymentMethods`,下面代码示例却写下划线式 `payment_methods`,到底哪个对说不清。 | [react-native](https://doc.komoju.com/docs/react-native.md) |
| 中高 | API字段/通用写作 | **字段名和示例对不上** | 文档表格说顶层字段叫 `object`,但所有 JSON 示例里用的都是 `resource`。按表格去解析会取不到值。 | [webhooks](https://doc.komoju.com/docs/webhooks.md) |
| 中高 | 通用写作 | **字段名漏了后缀** | 同一个参数,多数地方写 `processing_merchant_id`,个别地方写成 `processing_merchant`(漏了 `_id`),不一致。 | [seller-processing-model](https://doc.komoju.com/docs/seller-processing-model.md) |
| 中高 | 操作步骤/通用写作 | **必填/可选自相矛盾** | 文档先说「这些字段全部可选」,但下面表格里 `customer_ip`、`customer_email` 又各自标了「必填」,到底填不填矛盾。 | [fraud-details](https://doc.komoju.com/docs/fraud-details.md) |
| 中高 | 通用写作 | **版本号自相矛盾** | 同一篇更新日志,文件名和内部链接是 v2026-01-20,但标题和正文却写 v2026-01-27,到底是哪个版本对不上。 | [v2026-01-20](https://doc.komoju.com/changelog/v2026-01-20.md) |
| 中高 | API字段/通用写作 | **字段名不一致 + 单位不统一** | 表示「下次扣款时间」的字段,一处叫 `next_capture_at`,另一处叫 `next_capture_date`,同一概念两个名;金额单位在几处也对不上(小数 vs 整数)。 | [subscriptions](https://doc.komoju.com/reference/subscriptions.md) |
| 中高 | 跨文档一致性 | **举例用了不存在的状态** | 订阅状态官方生命周期只有 5 种,但另一篇的字段说明举例时用了 `cancelled`,而这个值根本不在那 5 种里,举错了。 | [subscriptions](https://doc.komoju.com/reference/subscriptions.md)<br>[showsubscription](https://doc.komoju.com/reference/showsubscription.md) |
| 中 | API字段/通用写作 | **字段名拼错了** | 一个金额字段名写成 `refunded_customer_fees_centr`,正确应是 `_cents`(少了 s);同一篇后面用的又是正确拼法。 | [unstable-more-details-in-responses-of-settlements-api-endpoints](https://doc.komoju.com/changelog/unstable-more-details-in-responses-of-settlements-api-endpoints.md) |
| 中 | 通用写作 | **标题跑题 + 拼写错** | 这篇讲「迁移到 Payment Links」,大标题却写成「Buys Deprecation(与主题无关)」;正文还有个拼写错误 retirted(应为 retired)。 | [migration-to-payment-links](https://doc.komoju.com/docs/migration-to-payment-links.md) |
