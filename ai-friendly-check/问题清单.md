# KOMOJU 文档问题清单（通俗版 · 精确定位）

> 对 KOMOJU 全套 164 篇英文文档的 AI 友好度质检，共发现 **31 条**问题（高 22 · 中高 7 · 中 2）。
> 每条给出两种定位：**① `.md` 原文位置**（点开是纯文本 `.md` 文件，可 Ctrl+F；下方直接摘录出错片段）；**② 渲染页**（不带 `.md` 的正式文档页，点开对照阅读）。
>
> 说明：渲染页的接口 / 示例（`reference/`、`recipes/`）字段默认折叠、且是 JS 动态生成，浏览器搜索经常搜不到，所以渲染页只给页面链接用于对照；要精确定位到出错那一行，请用左边的 `.md` 链接（纯文本，行号 + 摘录都在）。

| 严重度 | 类别 | 问题一句话 | 通俗说明 | .md 原文位置（附摘录） | 渲染页 |
|---|---|---|---|---|---|
| 高 | API字段/通用写作 | **字段名拼错了** | 响应里表示「条形码」的字段名写成了 `bardcode`，正确拼法是 `barcode`（少打了一处），而且旁边的说明写的就是「Barcode number」。程序按正确拼法去读会读不到值。这个错拼在 14 篇接口文档里都出现了。 | [reference/capturepayment.md 第1577行](https://doc.komoju.com/reference/capturepayment.md)<br>`"bardcode": {`<br>`  "type": "string",`<br>`  "description": "Barcode number for the CVS payment slip."` | [capturepayment](https://doc.komoju.com/reference/capturepayment) |
| 高 | API字段/通用写作 | **字段名拼错了** | iDEAL 支付明细里表示「交易码」的字段名写成 `transacion_key`，正确应是 `transaction_key`（漏了个 t）；同一篇里其他支付方式却是对的。这个错拼在 14 篇接口文档里都出现了。 | [reference/capturepayment.md 第2901行](https://doc.komoju.com/reference/capturepayment.md)<br>`"transacion_key": {`<br>`  "type": "string",`<br>`  "description": "Transaction key returned by iDEAL."` | [capturepayment](https://doc.komoju.com/reference/capturepayment) |
| 高 | API字段/通用写作 | **说明和实际对不上** | 韩国卡有个「银行卡密码前几位」的字段，字段名和格式限制都写明是「前 2 位」，但文字说明却写成「前 4 位」，自相矛盾，填几位说不清。 | [reference/updatepayment.md 第7537行](https://doc.komoju.com/reference/updatepayment.md)<br>`"first_two_digits_of_pin": {`<br>`  "minLength": 2,  "maxLength": 2,`<br>`  "description": "Specify the first four digit of credit card's PIN number."` | [updatepayment](https://doc.komoju.com/reference/updatepayment) |
| 高 | 通用写作 | **接口说明抄错了** | 「支付会话」这个接口，成功返回的说明却写成「Cancels the session.（取消会话）」。程序会误以为「成功=取消」，完全判反。 | [reference/paysession.md 第53行](https://doc.komoju.com/reference/paysession.md)<br>`// paySession 接口的 200 成功响应`<br>`"description": "Cancels the session.",` | [paysession](https://doc.komoju.com/reference/paysession) |
| 高 | 跨文档一致性/通用写作 | **接口说明抄错了** | 「查看文件」和「查看申请状态」两个查询接口，成功返回的说明都被错抄成「Creates a new file（新建文件）」，和接口实际功能完全不符。 | [reference/showfile.md 第50行](https://doc.komoju.com/reference/showfile.md)<br>`// showFile 接口的 200 成功响应`<br>`"description": "Creates a new file for its submerchant",`<br><br>[reference/showliveapplication.md 第50行](https://doc.komoju.com/reference/showliveapplication.md)<br>`// showLiveApplication 接口的 200 成功响应`<br>`"description": "Creates a new file for its submerchant",` | [showfile](https://doc.komoju.com/reference/showfile)<br>[showliveapplication](https://doc.komoju.com/reference/showliveapplication) |
| 高 | API字段/通用写作 | **可选值列漏了一个** | 查询支付列表时，能按状态筛选，但可选状态只列了 6 个，漏掉了 `failed`（失败）；而同一篇的数据定义里其实有这第 7 个状态。程序不知道能不能按「失败」筛。 | [reference/listpayments.md 第179行](https://doc.komoju.com/reference/listpayments.md)<br>`// 数据定义 PaymentStatus.enum 有 7 个(含 failed):`<br>`enum: [pending, authorized, captured, cancelled, expired, refunded, failed]`<br>`// 但 status 过滤参数的可选值只列了 6 个,漏掉 failed` | [listpayments](https://doc.komoju.com/reference/listpayments) |
| 高 | API字段 | **示例值不符合自己的规定** | 商户 ID 规定必须是 25 位，但文档给的示例值只有 22 位，违反了自己写的规则。 | [reference/listpayments.md 第133行](https://doc.komoju.com/reference/listpayments.md)<br>`"example": "merc4ezlzkh7gfp4g93853",   // 实际只有 22 字符`<br>`"description": "A unique 25-character alphanumeric merchant identifier."` | [listpayments](https://doc.komoju.com/reference/listpayments) |
| 高 | 跨文档一致性/通用写作 | **同一个词前后不一致** | 同一篇文档里，状态列表写成 `complete`，但代码和正文却写 `completed`（多了 d）。程序拿去比对字符串会对不上。 | [docs/hosted-page-integration-guide.md 第162行](https://doc.komoju.com/docs/hosted-page-integration-guide.md)<br>`* **"complete"**: the customer filled out the payment form ...`<br>`// 但同篇代码里判断的是 session.status === 'completed'(多个 d)` | [hosted-page-integration-guide](https://doc.komoju.com/docs/hosted-page-integration-guide) |
| 高 | 通用写作 | **同一字段两种类型** | 同一个 `capture` 字段，一处说它取值是「auto/manual」两个词，另一处又说它是 true/false 布尔值。两种定义互不兼容，不知道该传什么。 | [docs/creating-payments-directly.md 第122行](https://doc.komoju.com/docs/creating-payments-directly.md)<br>`\| payment_data[capture] \| enum \| "auto" or "manual". Defaults to "auto". \|`<br>`// 另一处却把 capture 描述成布尔值 true/false` | [creating-payments-directly](https://doc.komoju.com/docs/creating-payments-directly) |
| 高 | API字段/通用写作 | **三个字段说明一模一样** | `bank_id`、`customer_id`、`confirmation_id` 三个不同字段，说明文字完全相同（都写成同一句），读者根本分不清各自是干嘛的。 | [reference/payment-parameter-description.md 第227行](https://doc.komoju.com/reference/payment-parameter-description.md)<br>`bank_id:          A code required by customers when paying with Pay-easy.`<br>`customer_id:      A code required by customers when paying with Pay-easy.`<br>`confirmation_id:  A code required by customers when paying with Pay-easy.` | [payment-parameter-description](https://doc.komoju.com/reference/payment-parameter-description) |
| 高 | 代码示例 | **示例代码是坏的，跑不了** | 有 4 段示例请求代码里，金额键写成了 `"amount'`（左边双引号、右边单引号，引号配错），还有多余的逗号。任何程序去解析都会直接报错。 | [docs/platform-processing-model.md 第165行](https://doc.komoju.com/docs/platform-processing-model.md)<br>`"amount':"1000",     // 左双引号、右单引号,配对错误 + 多余逗号`<br>`"platform_fee":"100",` | [platform-processing-model](https://doc.komoju.com/docs/platform-processing-model) |
| 高 | 代码示例 | **示例代码是坏的，跑不了** | 同上，有 2 段示例请求代码里金额键 `"amount'` 引号配错并带多余逗号，复制过去无法运行。 | [docs/seller-processing-model.md 第320行](https://doc.komoju.com/docs/seller-processing-model.md)<br>`"amount':"850",      // 同样引号配对错误 + 多余逗号`<br>`"platform_fee":"50",` | [seller-processing-model](https://doc.komoju.com/docs/seller-processing-model) |
| 高 | 代码示例 | **示例里请求和返回对不上** | 示例请求填的名字是「Taro」，同一个示例的返回却显示「Tar」（少了个 o），对照会让人以为名字被截断了。 | [recipes/payment-details-example-bank-transfer.md 第56行](https://doc.komoju.com/recipes/payment-details-example-bank-transfer.md)<br>`// 请求填的是 "customer_given_name": "Taro"`<br>`"customer_given_name": "Tar",   // 响应里却少了个 o` | [payment-details-example-bank-transfer](https://doc.komoju.com/recipes/payment-details-example-bank-transfer) |
| 高 | 代码示例 | **示例里请求和返回对不上** | 示例请求填的邮箱是 customer@example.com，同一示例返回却是 test@example.com，前后对不上。 | [recipes/payment-details-example-konbini.md 第31行](https://doc.komoju.com/recipes/payment-details-example-konbini.md)<br>`// 请求填的是 "email": "customer@example.com"`<br>`"email": "test@example.com",    // 响应里却是另一个邮箱` | [payment-details-example-konbini](https://doc.komoju.com/recipes/payment-details-example-konbini) |
| 高 | 代码示例 | **示例里请求和返回对不上** | 示例请求填的卡有效期是 04/28，返回却显示成 1 月 / 2025 年，前后完全对不上，无法照着核对。 | [recipes/tokenize-credit-card-details.md 第68行](https://doc.komoju.com/recipes/tokenize-credit-card-details.md)<br>`// 请求填的是 month=04, year=28`<br>`"month": 1,`<br>`"year": 2025    // 响应里月/年完全对不上` | [tokenize-credit-card-details](https://doc.komoju.com/recipes/tokenize-credit-card-details) |
| 高 | 代码示例 | **示例 JSON 是坏的，跑不了** | 一个示例代码块里塞了两段独立的 JSON 数据，中间没有逗号也没用数组括起来，当成一个 JSON 解析必然报错。 | [recipes/tokenize-credit-card-details.md 第46行](https://doc.komoju.com/recipes/tokenize-credit-card-details.md)<br>`json Response Example`<br>`{ ... }   // 同一个 json 块里塞了两段独立 JSON`<br>`{ ... }   // 中间无逗号也没数组括起来 → 解析报错` | [tokenize-credit-card-details](https://doc.komoju.com/recipes/tokenize-credit-card-details) |
| 高 | 跨文档一致性 | **不同页面说法打架** | 「不填 tax 会怎样」在三处说法不一：两篇旧文档说「不填=默认按 10% 自动算税」，而更新日志（新版本）说「不填=税为 0」。看不同页会得出相反结论。 | [docs/creating-payments-directly.md 第407行](https://doc.komoju.com/docs/creating-payments-directly.md)<br>`\| tax \| integer \| The default is "auto" which is always 10% of your amount. \|`<br>`// 旧文档:不填 tax = 自动按 10% 算税`<br><br>[changelog/v2026-01-20.md 第22行](https://doc.komoju.com/changelog/v2026-01-20.md)<br>`if tax is not included in the request the calculated tax will be set to 0.`<br>`// 新版更新日志:不填 tax = 税为 0(与旧文档相反)` | [creating-payments-directly](https://doc.komoju.com/docs/creating-payments-directly)<br>[v2026-01-20](https://doc.komoju.com/changelog/v2026-01-20) |
| 高 | 跨文档一致性 | **状态清单缺一个** | 支付状态，权威的数据定义有 7 个（含 `failed` 失败），但给人看的状态说明表只列了 6 个，漏了「失败」。判断支付状态时会漏掉这一种。 | [reference/payments.md 第13行](https://doc.komoju.com/reference/payments.md)<br>`# Payment Statuses`<br>`// 给人看的状态说明表只列了 6 个,漏了 failed`<br>`// 而 schema(listpayments)里 enum 有 7 个(含 failed)`<br><br>[reference/listpayments.md 第179行](https://doc.komoju.com/reference/listpayments.md)<br>`enum: [pending, authorized, captured, cancelled, expired, refunded, failed]`<br>`// schema 有 7 态,含 failed` | [payments](https://doc.komoju.com/reference/payments)<br>[listpayments](https://doc.komoju.com/reference/listpayments) |
| 高 | 跨文档一致性 | **两篇不同文档标题撞车** | 两篇主题不同的集成指南（一篇讲托管页、一篇讲前端字段渲染）用了完全一样的大标题「Integration Guide: Customer」，检索时分不清是哪一篇、容易取错。 | [docs/integration-guide-customer.md 第7行](https://doc.komoju.com/docs/integration-guide-customer.md)<br>`# Integration Guide: Customer`<br>`// 本篇讲:用托管页收集 token(Hosted Page)`<br><br>[docs/integration-guide-customer-1.md 第7行](https://doc.komoju.com/docs/integration-guide-customer-1.md)<br>`# Integration Guide: Customer   ← 与另一篇 H1 完全相同`<br>`// 但本篇讲:用 KOMOJU Fields 收集 token(前端字段)` | [integration-guide-customer](https://doc.komoju.com/docs/integration-guide-customer)<br>[integration-guide-customer-1](https://doc.komoju.com/docs/integration-guide-customer-1) |
| 中高 | 通用写作 | **链接指错了地方** | 文档里 3 处跳转链接的地址拼错或指向了错误的接口页（比如把「创建会话」拼成了 creatession），点进去到不了正确页面。 | [docs/creating-payments-directly.md 第106行](https://doc.komoju.com/docs/creating-payments-directly.md)<br>`Call the [Session: Create](/reference/creatession) API ...`<br>`// 锚点拼错:creatession 应为 createsession,点进去 404` | [creating-payments-directly](https://doc.komoju.com/docs/creating-payments-directly) |
| 中高 | 通用写作 | **错误码表前后不一致** | 同一篇里有两张状态码表，401 的叫法不一样（一处 Not Authorized、一处 unauthorized）；而且 HTTP 表还漏掉了 400/402/504 三个错误码。 | [docs/errors.md 第18行](https://doc.komoju.com/docs/errors.md)<br>`\| 401 Not Authorized \| ... \|      ← 第一张表这样叫`<br>`\| unauthorized \| 401 \| ... \|      ← 第二张表又叫 unauthorized` | [errors](https://doc.komoju.com/docs/errors) |
| 中高 | 通用写作 | **同一样东西两个叫法** | 同一个凭证，第一步叫「Merchant ID」，后面又叫「Merchant UUID」，读者不确定是不是同一个东西。 | [docs/getting-started-with-ec-cube.md 第78行](https://doc.komoju.com/docs/getting-started-with-ec-cube.md)<br>`* **Merchant ID** — your account identifier   ← 第 1 步叫 Merchant ID`<br>`// 后文同一凭证又叫 Merchant UUID` | [getting-started-with-ec-cube](https://doc.komoju.com/docs/getting-started-with-ec-cube) |
| 中高 | 通用写作 | **参数名前后不一致** | 同一个参数，说明表里写驼峰式 `paymentMethods`，下面代码示例却写下划线式 `payment_methods`，到底哪个对说不清。 | [docs/react-native.md 第76行](https://doc.komoju.com/docs/react-native.md)<br>`\| paymentMethods \| Array<PaymentTypes> \| ... \|   ← 参数表用驼峰`<br>`// 下面代码示例却写 payment_methods(下划线)` | [react-native](https://doc.komoju.com/docs/react-native) |
| 中高 | API字段/通用写作 | **字段名和示例对不上** | 文档表格说顶层字段叫 `object`，但所有 JSON 示例里用的都是 `resource`。按表格去解析会取不到值。 | [docs/webhooks.md 第131行](https://doc.komoju.com/docs/webhooks.md)<br>`"resource": "event",   ← 所有 JSON 示例用 resource`<br>`// 但上面字段表说顶层字段叫 object` | [webhooks](https://doc.komoju.com/docs/webhooks) |
| 中高 | 通用写作 | **字段名漏了后缀** | 同一个参数，多数地方写 `processing_merchant_id`，个别地方写成 `processing_merchant`（漏了 `_id`），不一致。 | [docs/seller-processing-model.md 第41行](https://doc.komoju.com/docs/seller-processing-model.md)<br>`* processing_merchant_id: The ID of the Seller Merchant ...   ← 多数处`<br>`// 个别处写成 processing_merchant(漏了 _id)` | [seller-processing-model](https://doc.komoju.com/docs/seller-processing-model) |
| 中高 | 操作步骤/通用写作 | **必填/可选自相矛盾** | 文档先说「这些字段全部可选」，但下面表格里 `customer_ip`、`customer_email` 又各自标了「必填」，到底填不填矛盾。 | [docs/fraud-details.md 第19行](https://doc.komoju.com/docs/fraud-details.md)<br>`Note that all of these are optional unless stated otherwise.   ← 对象级说全可选`<br>`// 但下面表格里 customer_ip / customer_email 又各自标了「必填」` | [fraud-details](https://doc.komoju.com/docs/fraud-details) |
| 中高 | 通用写作 | **版本号自相矛盾** | 同一篇更新日志，文件名和内部链接是 v2026-01-20，但标题和正文却写 v2026-01-27，到底是哪个版本对不上。 | [changelog/v2026-01-20.md 第22行](https://doc.komoju.com/changelog/v2026-01-20.md)<br>`文件名 / 内链: v2026-01-20`<br>`// 但标题和正文却写 v2026-01-27` | [v2026-01-20](https://doc.komoju.com/changelog/v2026-01-20) |
| 中高 | API字段/通用写作 | **字段名不一致 + 单位不统一** | 表示「下次扣款时间」的字段，一处叫 `next_capture_at`，另一处叫 `next_capture_date`，同一概念两个名；金额单位在几处也对不上（小数 vs 整数）。 | [reference/subscriptions.md 第92行](https://doc.komoju.com/reference/subscriptions.md)<br>`\| next_capture_at \| Time at which next interval starts ... \|   ← 一处叫 _at`<br>`// 另一处(第 129 行)却写 next_capture_date` | [subscriptions](https://doc.komoju.com/reference/subscriptions) |
| 中高 | 跨文档一致性 | **举例用了不存在的状态** | 订阅状态官方生命周期只有 5 种，但另一篇的字段说明举例时用了 `cancelled`，而这个值根本不在那 5 种里，举错了。 | [reference/subscriptions.md 第102行](https://doc.komoju.com/reference/subscriptions.md)<br>`状态生命周期只有 5 种: pending / active / retrying / suspended / cancelled?`<br>`\| pending   \| Initialized and ready to capture first payment. \|`<br><br>[reference/showsubscription.md 第129行](https://doc.komoju.com/reference/showsubscription.md)<br>`"description": "Current status of the subscription (e.g. \"active\", \"cancelled\")."`<br>`// 举例用了 cancelled,但它不在权威 5 态生命周期里` | [subscriptions](https://doc.komoju.com/reference/subscriptions)<br>[showsubscription](https://doc.komoju.com/reference/showsubscription) |
| 中 | API字段/通用写作 | **字段名拼错了** | 一个金额字段名写成 `refunded_customer_fees_centr`，正确应是 `_cents`（少了 s）；同一篇后面用的又是正确拼法。 | [changelog/unstable-more-details-in-responses-of-settlements-api-endpoints.md 第147行](https://doc.komoju.com/changelog/unstable-more-details-in-responses-of-settlements-api-endpoints.md)<br>`refunded_customer_fee_total now called refunded_customer_fees_centr   ← 拼错`<br>`// 应为 _cents;同篇后文用的又是正确拼法` | [unstable-more-details-in-responses-of-settlements-api-endpoints](https://doc.komoju.com/changelog/unstable-more-details-in-responses-of-settlements-api-endpoints) |
| 中 | 通用写作 | **标题跑题 + 拼写错** | 这篇讲「迁移到 Payment Links」，大标题却写成「Buys Deprecation（与主题无关）」；正文还有个拼写错误 retirted（应为 retired）。 | [docs/migration-to-payment-links.md 第7行](https://doc.komoju.com/docs/migration-to-payment-links.md)<br>`# Buys Deprecation   ← 大标题与「迁移到 Payment Links」主题不符`<br>`KOMOJU Buys has been retirted in favor ...   ← retirted 应为 retired` | [migration-to-payment-links](https://doc.komoju.com/docs/migration-to-payment-links) |
