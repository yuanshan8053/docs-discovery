# 交付纪律（Delivery Discipline）

本仓库所有交付物（`.md` 文档）在**提交 / 推送前必须通过中文书写规范门**，无一例外。这是一条硬纪律，不是建议。

## 唯一门禁：中文书写规范 lint

**规则**：中文正文中，标点必须使用全角（，。；：！？（）等）。凡半角标点 `, . ; : ! ? ( )` **紧邻一个 CJK 汉字**（前一个或后一个字符是汉字），即判为残留，必须改为全角。

**豁免**（合法半角，门禁自动跳过，不误报）：

- 围栏代码块 ` ``` … ``` ` 内整段。
- 行内代码 `` `…` `` 内容（字段名、枚举字面量、`3.0.2` 等）。
- Markdown 链接的 URL 段 `](…)` 与裸 URL `http(s)://…`。
- 数字/英文之间的半角（如 `1,000`、`403/404`、`errors.md`、`10:30`）——两侧非汉字，天然不命中。

## 每次交付前必做（两步）

```bash
# 1) 门禁自检：退出码 0 = 干净放行；1 = 有残留，禁止提交
python3 scripts/zh_punct_lint.py <改动的 .md 文件...>

# 2) 若报残留，用配套修复器就地转全角，再回到第 1 步复检至 [PASS]
python3 scripts/zh_punct_fix.py <改动的 .md 文件...>
```

- `scripts/zh_punct_lint.py` —— 门禁。逐行扫描，命中即 `file:line:col` 定位并打印上下文；退出码 `0` 干净 / `1` 有残留 / `2` 用法错，可直接接进 CI 或提交前 hook 当机器收敛判据。
- `scripts/zh_punct_fix.py` —— 配套修复器。与门禁同源的掩码逻辑（跳过代码块、行内代码、链接 URL、裸 URL），只改中文正文、不动代码；圆括号按配对处理，避免半开全闭错配。

## 已知坑（务必回避）

- **文件名 / 扩展名紧邻汉字时会被误转**（如《`问题清单.md`》里的 `.md` → `。md`）。凡在中文正文里出现的裸文件名 / 路径 / 锚点，一律用行内代码 `` `…` `` 包起来，门禁与修复器都会跳过，既不误报也不误改。
- 修复器为**就地改写**，改完务必 `git diff` 核对，确认没有把代码 / URL / 文件名改坏，再提交。

## 教训来源

最早交付的 `ai-friendly-check/问题清单.md` 在纪律确立前推送，事后 lint 查出 **118 处**半角标点残留。此后本纪律确立：**门禁不过，不予交付。**
