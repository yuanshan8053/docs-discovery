# KOMOJU 文档专业性 & 帮助性提升清单（通俗版 · 精确定位）

> 本清单是对《KOMOJU 文档问题清单（通俗版 · 精确定位）》AI 友好度质检的**补充**，视角不同、内容**不重复**：质检报告聚焦「拼写错、抄错、示例对不上、坏 JSON、前后矛盾」这类**点状错误**；本清单站在 **API 使用者（开发者拿文档去接接口）** 的立场，聚焦「读不懂、不会填、缺依据」这类**专业性与帮助性的系统缺口**——即字段普遍无说明、枚举普遍不讲含义、错误码/限流普遍不写。这些不算「错」，但同样实实在在地挡住使用者。
>
> 评审依据 api-doc-agent 技能的写作准则：① 每个字段要讲清「传什么/为什么传/怎么取」;② 枚举要逐值解释**业务含义**（取值字面量归结构，含义归说明）;③ 请求/返回标量字段要有满足自身约束的**示例值**;④ 拿不准的硬事实（默认值、错误码、限流）应显式补齐而非留白。
>
> **去重声明**：质检报告已提出的问题（`bardcode`/`transacion_key` 拼错、`first_two_digits_of_pin` 说明矛盾、`paySession`/`showFile` 抄错、示例请求/返回对不上、坏 JSON、`capture` 类型冲突、`tax` 默认矛盾、支付状态 6 vs 7、`next_capture_at`/`_date` 命名、标题撞车、`bank_id` 三字段同说明、错误码表叫法不一等），本清单**一律不再收录**。
>
> **定位方式**（沿用质检报告）：每条给两种定位——**① `.md` 原文位置**（纯文本文件，可 Ctrl+F，附行号 + 摘录）;**② 渲染页**（正式文档页，点开对照）。渲染页接口/示例字段默认折叠且 JS 动态生成，浏览器常搜不到，故精确定位请以左侧 `.md` 行号为准。
>
> 本清单共 **9 条**提升项（高 4 · 中高 4 · 中 1）。**改进建议已并入表格最右列**，可逐条对照修复。行号均逐条读原文核验。

| 严重度 | 类别 | 问题一句话 | 通俗说明（为什么挡住使用者） | `.md` 原文位置（附摘录） | 渲染页 | 改进建议 |
|---|---|---|---|---|---|---|
| 高 | API字段/帮助性 | **最核心的「支付状态」字段全程零说明** | `PaymentStatus` 是每次查支付/建支付都会读到的最关键字段，它列了 7 个取值却把 `description` 留成空串 `""`；全套 12+ 篇支付/会话接口文档里，没有一句话解释这个字段是干嘛的。开发者只看到一串状态词，不知道字段用途、也无从下手写状态判断。 | [reference/createpayment.md 第156行](https://doc.komoju.com/reference/createpayment.md)<br>`"PaymentStatus": {`<br>`  "enum": ["pending","authorized","captured","cancelled","expired","refunded","failed"],`<br>`  "example": "captured",`<br>`  "description": ""`<br>（同样空说明：[showpayment.md 第124行](https://doc.komoju.com/reference/showpayment.md)、[refundpayment.md 第154行](https://doc.komoju.com/reference/refundpayment.md)、paysession.md、showsession.md 等 12+ 篇） | [createpayment](https://doc.komoju.com/reference/createpayment)<br>[showpayment](https://doc.komoju.com/reference/showpayment) | 建议在 `reference/payments.md` 建一份**权威状态说明**，给 `description` 写清字段用途，各接口引用该处，杜绝再度漂移。 |
| 高 | 枚举无含义/帮助性 | **支付状态 7 个取值没有一个解释含义** | 承上：`pending / authorized / captured / cancelled / expired / refunded / failed` 只有字面量，没告诉开发者「`authorized`（已授权未扣款）和 `captured`（已扣款）差在哪」「`expired` 和 `cancelled` 有何区别」。程序员只能靠猜写状态机，极易判错。 | [reference/createpayment.md 第158行](https://doc.komoju.com/reference/createpayment.md)<br>`"enum": ["pending","authorized","captured",`<br>`"cancelled","expired","refunded","failed"]`<br>`// 7 个取值，无逐值业务含义说明` | [createpayment](https://doc.komoju.com/reference/createpayment) | 逐值补业务含义，如 `authorized`=已授权待扣款、`captured`=已扣款、`expired`=支付超时失效、`refunded`=已退款、`failed`=支付失败。取值字面量保留在 `enum`，含义写进说明。 |
| 高 | 枚举无含义/帮助性 | **「会话状态」字段连 description 键都没有** | `SessionStatus` 列了 `pending / completed / cancelled` 三态，整个字段对象**连 `description` 键都不存在**，一个字的解释都没有。托管页/会话回调场景全靠这个状态判断支付结果，却无从得知每态含义与触发时机。 | [reference/createsession.md 第7194行](https://doc.komoju.com/reference/createsession.md)<br>`"SessionStatus": {`<br>`  "type": "string",`<br>`  "enum": ["pending","completed","cancelled"]`<br>`}   // 无 description 键`<br>（showsession.md、paysession.md 同） | [createsession](https://doc.komoju.com/reference/createsession) | 补 `description` 键并逐值解释 `pending`（待完成）/`completed`（已完成）/`cancelled`（已取消）的含义与触发时机。 |
| 高 | 错误码缺失/帮助性 | **每个接口都不列本接口的专属错误码** | 抽查各接口只声明泛化的 403/404/422/503，从不列出本接口真实会返回的业务 `code`（如退款的 `not_refundable`、扣款的 `not_capturable`、取消的 `not_cancellable`——这些在 `docs/errors.md` 明明存在）。开发者不知道该处理哪些业务错误，异常分支只能瞎写。 | [reference/refundpayment.md 第62/72/82/92行](https://doc.komoju.com/reference/refundpayment.md)<br>`"403" / "404" / "422" / "503"   // 仅泛化 4 个`<br>`// 无 not_refundable 等本接口专属 code`<br>[docs/errors.md 第66行](https://doc.komoju.com/docs/errors.md)<br>`not_refundable / not_capturable / not_cancellable   // 存在却无接口引用` | [refundpayment](https://doc.komoju.com/reference/refundpayment)<br>[errors](https://doc.komoju.com/docs/errors) | 为每个接口补本接口真实会触发的业务 `code` 及触发条件；拿不准的显式标注「待研发补充」，不留空、不编造。 |
| 中高 | 枚举无含义/帮助性 | **会话模式三个取值没讲清各自场景** | `SessionMode`（`payment` / `customer` / `customer_payment`）只有一句笼统说明「可创建支付或客户资源」，没有逐值说明「`customer_payment` 与 `customer` 到底差在哪、各自适用什么业务场景」。开发者选错模式会导致该收款没收、该建档没建。 | [reference/createsession.md 第7099行](https://doc.komoju.com/reference/createsession.md)<br>`"SessionMode": {`<br>`  "enum": ["payment","customer","customer_payment"],`<br>`  "description": "Specifies what to do ... Can create a payment, or a customer resource."` | [createsession](https://doc.komoju.com/reference/createsession) | 逐值展开：`payment`=仅创建支付（默认）、`customer`=仅建/更新客户资源、`customer_payment`=收款同时建档，并各配一句适用场景。 |
| 中高 | 错误响应缺失/帮助性 | **部分核心接口连错误响应都不声明** | `createSession`、`showSession`、`createCustomer` 等核心接口的 OpenAPI 里**没有任何 4xx/5xx 响应条目**——开发者完全不知道失败时返回什么结构、什么状态码，无法预先写好错误处理。 | [reference/createsession.md](https://doc.komoju.com/reference/createsession.md)<br>`// responses 下仅有 200，无任何 4xx/5xx`<br>（showsession.md、createcustomer.md 同，经核 4xx/5xx 条数均为 0） | [createsession](https://doc.komoju.com/reference/createsession) | 为这些接口补齐 4xx/5xx 响应结构与状态码，与其它接口的错误响应对齐。 |
| 中高 | 限流缺失/帮助性 | **74 篇里只有 1 篇提到 429，且全库无限流说明** | `429`（请求过多）响应只在 `createpayment.md` 一篇里定义，其余 73 篇均无；`docs/`、`recipes/` 里**没有任何限流/退避重试说明文档**。开发者不知道限流阈值、不知道 429 该如何重试，生产上极易被限流打挂。 | [reference/createpayment.md 第86行](https://doc.komoju.com/reference/createpayment.md)<br>`"429": { ... "Rate-limiting" }   // 全库唯一`<br>`// docs/、recipes/ 无任何限流指南` | [createpayment](https://doc.komoju.com/reference/createpayment) | 在 `docs/` 新增「Rate Limiting」页，说明限流阈值、`429` 响应结构与退避重试策略；并把 `429` 响应补进各写操作接口。 |
| 中高 | 枚举无含义/帮助性 | **分期字段取值只列数字，不讲约束与 revolving** | `Installments` 列了 `3/5/6/10/12/15/18/20/24/revolving`，说明只笼统提「部分收单机构、仅限 `JPY`」，没讲清 `revolving`（循环付款）与按月分期的行为差异，也没说明哪些收单机构支持哪些期数。开发者传了不支持的值才在运行时报错。 | [reference/createsession.md 第174行](https://doc.komoju.com/reference/createsession.md)<br>`"Installments": { "enum": ["3",...,"24","revolving"],`<br>`  "description": "... only works for some acquirers, and only for \`JPY\` transactions."` | [createsession](https://doc.komoju.com/reference/createsession) | 解释 `revolving` 与按月分期的行为差异；若各收单机构支持期数不同，列出对应关系或标注「待研发补充」。 |
| 中 | 枚举无含义/帮助性 | **退款用银行账户类型三值未解释** | 退款请求里 `account_type`（`normal` / `checking` / `savings`）只写「客户银行账户类型」，没解释日本银行语境下 `normal`（普通预金）、`checking`（当座预金）、`savings`（储蓄预金）各指什么。填错账户类型会导致退款打款失败。 | [reference/createrefundrequest.md 第223行](https://doc.komoju.com/reference/createrefundrequest.md)<br>`"account_type": { "enum": ["normal","checking","savings"],`<br>`  "description": "Type of the customer's bank account."` | [createrefundrequest](https://doc.komoju.com/reference/createrefundrequest) | 逐值补日本银行账户语义：`normal`=普通预金、`checking`=当座预金、`savings`=储蓄预金。 |

---

## 小结

- **最大的帮助性缺口是「成规模的空白」**：核心字段 `PaymentStatus` / `SessionStatus` 全程无说明、关键枚举只列取值不讲含义、几乎所有接口不列专属错误码、全库无限流说明。这些让开发者无法仅凭文档正确、安全地接接口——是本轮最应优先补齐的部分，也与质检报告的点状纠错互补。
- **建议建立「单一真相源」**：把状态、枚举、错误码集中到权威说明页（如 `reference/payments.md`），各接口引用，从机制上防止说明缺失与再度漂移。
- 所有行号已逐条读原文核验；与《问题清单（通俗版 · 定位增强）》的 31 条问题**无重复**。
